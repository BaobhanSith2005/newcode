fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
pillow==11.1.0
httpx==0.28.1
# numpy 放成区间：rapidocr 要 numpy<2，新版 onnxruntime 又要 numpy≥2，
# 钉死哪个都会打架，让 pip 自己挑兼容的
numpy>=1.26.4,<3

# 漫画嵌字两件套：
# rapidocr_onnxruntime：气泡文字检测（OCR 模型内置在包里，不用另下）
rapidocr_onnxruntime>=1.3.24
# onnxruntime-gpu：LaMa 擦除模型的运行时（GPU 加速）。
# 安装顺序有讲究：rapidocr 会自带 CPU 版 onnxruntime，GPU 版排在后面装，
# 装的时候覆盖同名模块文件 = 自动用上你的 RTX 3050 Ti；
# 没 GPU 的机器跑会报 provider 错误，但我们代码里已配了 CPU 自动回退
onnxruntime-gpu>=1.19.0
